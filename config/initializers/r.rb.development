if !defined? R
  class R
    # 静态文件url的前缀
    # 见 RUtil.get_static_file_url(path)
    STATIC_FILE_URL_PREFIX = "static_files"

    # 静态文件硬盘路径的前缀
    # 见 RUtil.get_static_file_path(path)
    UPLOAD_FILE_BASE_PATH = Rails.root.join("public/static_files")

    # aliyun oss 需要的配置(目前版本因为没有使用 oss，所以没有用到这些配置)
    #（目前没有任何代码引用了这几个参数）
    ALIYUN_OSS_ACCESS_KEY_ID = ""
    ALIYUN_OSS_ACCESS_KEY_SECRET = ""
    ALIYUN_OSS_HOST = ""
    ALIYUN_OSS_BUCKET_NAME = ""
  end
end
