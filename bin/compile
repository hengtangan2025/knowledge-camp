#!/bin/sh

echo '开始签出 git submodule'
git submodule init
git submodule update

echo '开始 bundle'
bundle install --without development:test --path vendor/bundle --binstubs vendor/bundle/bin -j4 --deployment

echo '开始构建 assets'
rake assets:precompile

