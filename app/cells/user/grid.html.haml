.cell-users.grid.row
  - @users.each do |user|
    .user
      %a.box{:href => url_for([:old, :manage, user]), :data => {:toggle => true}}
        .avatar= avatar user, :large
        .login= user.login
        .name= user.name

  .col-pad-12
    = paginate @users
