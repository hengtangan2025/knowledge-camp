.course.course-one-detail.clearfix{data: {course_id: @course.id}}
  .cover.pull-left
    %a{href: bank_course_path(@course)}
      %img.img-responsive{src: (@course.cover.blank? ? 'http://ph.4ye.me/440/248' : @course.cover)}
  .course-right
    .info
      %h3.title
        %a{href: bank_course_path(@course)}= @course.title
      .desc= @course.desc

      .bottom
        .progress-info.pull-left
          .to 
            已学习至：
            %span.current-ware-title
          .cost
            学习耗时：
            %span.course-spent-time
          .last
            最后学习：
            %span.course-last-studied-at
          .progress
            .progress-bar{role: 'progressbar', aria: {valuenow: @percent, valuemin: '0', valuemax: '100'}}
        .continue_study
          %a.learn.btn.btn-default.btn-lg.pull-right{href: (study_bank_course_path(@course))}
            继续学习
        .studied
          %a.learn.btn.btn-default.btn-lg.pull-right{href: (study_bank_course_path(@course))}
            已学完
        .start_study
          %a.learn.btn.btn-default.btn-lg.pull-right{href: (study_bank_course_path(@course))}
            开始学习
        .clearfix

      - if @show_share_and_fav
        .share_and_fav
          .bucketerize.pull-right{data: {rel: "bucketerize", bucketerize: {mode: 'standard', resource_type: 'KcCourses::Course', resource_id: @course.id}}}
            .unhas
              %button.btn.btn-default 
                %span.glyphicon.glyphicon-heart-empty
                收藏
            .has
              %button.btn.btn-info
                %span.glyphicon.glyphicon-heart-empty
                已收藏
          .share.pull-right
            %button.btn.btn-default
              %span.glyphicon.glyphicon-share
              分享
