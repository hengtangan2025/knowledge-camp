.cell-manage-nets.grid.row
  - @nets.each do |net|
    .net
      %a.box{:href => url_for([:manage, net]), :data => {:toggle => true}}
        .name= net.name
        .desc= net.desc
        .updated-at 更新于 #{net.updated_at.to_s(:db)}
        .stat
          .points 知识点 #{net.points.count}