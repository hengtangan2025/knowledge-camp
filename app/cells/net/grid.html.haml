.cell-manage-nets.grid.row
  - @nets.each do |net|
    .net
      .box
        .name= link_to net.name, [:manage, net]
        .desc= net.desc
        .updated-at 更新于 #{net.updated_at.to_s(:db)}