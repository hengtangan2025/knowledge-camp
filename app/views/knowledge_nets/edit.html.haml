.page-net-new
  = simple_form_for @net, :url => "/knowledge_nets/#{@net.id}" do |f|
    .field= f.input :name, :placeholder => "名称"
    .field= f.input :desc, :placeholder => "描述", :as => :text
    .field= f.submit "修改"
    = link_to "取消", "/knowledge_nets"