.page-net-form
  .col-pad-6.col-pad-offset-1
    %h2.cell 新建知识网络 …
    = simple_form_for @net, :url => "/knowledge_nets/#{@net.id}", :defaults => {:input_html => {:class => 'form-control'}} do |f|
      .form-group
        = f.input :name, :placeholder => "名称", :label => false
      .form-group
        = f.input :desc, :placeholder => "描述", :label => false, :as => :text, :input_html => {:rows => 10}
      .form-group
        = f.submit
        %a.cancel{:href => '/'} 取消