- url = "/bank/manage/model_labels"

.page-bank-manage-model-label-new
  .well
    = "给#{params[:name]}增加标签"
  = simple_form_for @model_label, :url => url do |f|
    = f.input :model, :as => :hidden
    = f.input :name
    = f.input :values
    = f.button :submit
