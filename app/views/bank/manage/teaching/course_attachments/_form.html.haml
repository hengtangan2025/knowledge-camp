= simple_form_for [:bank, :manage, @course, @course_attachment] do |f|
  .form-inputs
    = f.input :file_entity_id, as: :hidden

    %a.btn-upload-attachment.btn.btn-info.clearfix{href: 'javascript:;', data: FilePartUpload.get_dom_data} 上传资料

    .result
      上传结果：

      #upload_result

  %br
  .form-actions
    %button.btn.btn-primary
      %i.glyphicon.glyphicon-ok{type: 'submit'}
      保存
    %span.extra_buttons
      %a.btn.btn-default{type: 'button', href: url_for([:bank, :manage, @course])}
        %i.glyphicon.glyphicon-remove
        取消

