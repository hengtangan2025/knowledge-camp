:ruby
  bread_data = [
    [ :net, @net.name, url_for([:old, :manage, @net]) ],
    [ :forms, '查看所有知识点', url_for([:old, :manage, @net, :points]) ]
  ]

.page-main.col-pad-10.col-pad-offset-1
  = bread bread_data do
    .point-form
      = dandelion_form :old, :manage, @net, @point
