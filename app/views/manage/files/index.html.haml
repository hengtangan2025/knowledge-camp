:ruby
  bread_data = [
    [ :net, @net.name, url_for([:manage, @net]) ],
  ]

.page-side.col-pad-1

.page-main.col-pad-10
  = bread bread_data do
    .files
      .title 查看所有文件
      = render_cell :file, :grid, @virtual_files.limit(20)