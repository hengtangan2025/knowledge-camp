:ruby
  bread_data = [
    [ :net, @net.name, url_for([:manage, @net]) ],
  ]

.page-side.col-pad-1

.page-main.col-pad-10
  = bread bread_data do
    .files
      .title 查看所有文件
      - paged_files = @virtual_files.page(params[:page]).per(24)

      = render_cell :file, :grid, paged_files
      = paginate paged_files