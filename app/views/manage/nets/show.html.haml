- page_title @net.name

.page-side.col-pad-2
  .nav{:data => {:'fixtop-offset' => 30}}
    .back
      %a{:href => url_for([:manage, :nets]), :data => {:toggle => true}}
        %i.fa.fa-arrow-left
        %span 退出

    - [:point, :document, :file, :plan].each do |model_name|
      = render_cell :net, :nav_items, @net, model_name

.page-main.col-pad-10
  %section.net
    .name= @net.name
    .desc= @net.desc

  %section
    %a.btn.btn-rounded.btn-bdb-dream{:href => url_for([:graph, :manage, @net])}
      %span 预览知识网络

  - [:point, :document, :file].each do |model_name|
    = render_cell :net, :model_section, @net, model_name