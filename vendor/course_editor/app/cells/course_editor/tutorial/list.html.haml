.tutorials.ce.list
  - @tutorials.each do |tutorial|
    %a.tutorial{:href => @view.url_for([:design, tutorial])}
      .row
        .col-pad-2
          = fit_ibox tutorial.image.url
        .col-pad-6
          .title= tutorial.title
          = text_div :desc, tutorial.desc, '没有描述信息'
        .col-pad-4
          .author
            .avatar= avatar_with_size tutorial.creator, 40
            .name
              %span 作者：
              %span #{tutorial.creator.name}
            .time
              %span 编辑于：
              %span #{tutorial.updated_at.to_s(:db)}
            .length
              %span 篇幅：
              %span #{tutorial.steps.count}
              %i.fa.fa-file-text-o
              %span 个页面