.row
  .page-main.col-pad-10.col-pad-offset-1
    .page-bread-pieces
      .bread.net
        .bread-link
          %a{:href => "/manage/nets/#{@net.id}"}= @net.name
        .bread.forms
          .bread-link
            %a{:href => url_for(@net)} 查看相关教学主题
          .bread.current
            .plan-store-item-form
              %h2.cell 新建教学主题
              = render "form"
