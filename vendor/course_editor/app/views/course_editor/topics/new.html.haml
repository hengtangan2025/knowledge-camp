:ruby
  bread_data = [
    [ :'net-topics', "#{@net.name} - 教学主题管理", url_for([@net])]
  ]

.col-pad-12
  = bread bread_data, true do
    .piece.piece-shelf
      .piece.piece-box
        %h4 创建教学主题
        = render 'form'