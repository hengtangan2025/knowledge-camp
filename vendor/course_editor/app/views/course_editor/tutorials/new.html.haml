:ruby
  bread_data = [
    [ :'net-topics', "#{@net.name} - 教学主题管理", url_for([@net])],
    [ :topic, @topic.title, url_for(@topic)]
  ]

.col-pad-12
  = bread bread_data, true do
    .piece.piece-shelf
      .piece.piece-box
        %h4 创建教程
        = render "form"