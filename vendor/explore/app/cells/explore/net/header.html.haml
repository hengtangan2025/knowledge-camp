- color = string_grid_color @net.id

.header{:style => "background:#{color}"}
  %a.back{:href => root_url}
    %i.fa.fa-arrow-left
  .name= @net.name
  - if @net.desc.present?
    .desc= @net.desc
  - else
    .desc 没有简介信息