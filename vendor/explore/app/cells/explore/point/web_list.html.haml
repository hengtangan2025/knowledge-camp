.points.web.list
  - if @points.blank?
    .piece-blank{:style => 'margin:0 10px;'} 没有相关知识点

  - else
    - @points.each do |point|
      - url = "/explore/points/#{point.id}"
      %a.point{:href => url}
        :ruby
          bg = string_grid_color(point.id)

        .icon{:style => "border-color:#{bg}"}
        .name-box
          .name
            %span= point.name