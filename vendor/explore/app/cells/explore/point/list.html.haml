.cell-explore-points.list
  - if @points.blank?
    .point.blank
      .icon
      .name-box
        .name 没有了

  - else
    - @points.each do |point|
      - url = e_old_explore.point_path(point.id)
      %a.point{:href => url}
        :ruby
          bg = string_grid_color(point.id)
          bg1 = ApplicationHelper::ColorTransfer.darken(bg.to_s)

        .icon{:style => "border-color:#{bg}"}
        .name-box
          .name
            %span= point.name
