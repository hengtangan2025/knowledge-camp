.tutorials.learning.web
  - @tutorials.each do |tutorial|
    - progress = tutorial.progress_by @user

    .tutorial
      %a.tbox{:href => e_old_explore.tutorial_path(tutorial.id)}
        .img
          .ibox= fit_image tutorial.image.url, :width => '100%', :height => '100%'
        .textinfo
          .title= tutorial.title
          - if tutorial.desc.blank?
            .desc.blank 没有描述信息
          - else
            .desc= tutorial.desc
        .progress
          .bar
            .p{:style => "width:#{progress}%"}
          .flag
            .percent #{progress}%
            .done
              %i.fa.fa-check
