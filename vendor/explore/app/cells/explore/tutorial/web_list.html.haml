.tutorials.web.list
  - @tutorials.each do |tutorial|
    .toturial.piece.piece-box
      %a.tbox{:href => e_old_explore.tutorial_path(tutorial.id)}
        .row
          .col-pad-3
            .img
              .ibox= fit_image tutorial.image.url, :width => '100%', :height => '100%'
          .col-pad-6
            %h4.title= tutorial.title
            - if tutorial.desc.blank?
              .desc.blank 没有介绍信息
            - else
              .desc= tutorial.desc
          .col-pad-3
            .author
              - if tutorial.creator.present?
                .avatar= avatar tutorial.creator
                .name= tutorial.creator.name
                .time
                  %span 发布于
                  %span= tutorial.created_at.to_s :short1

          .progress.col-pad-6
            .bar
              .p{:style => "width:#{50}%"}
            .flag
              .percent 50%
              .done
                %i.fa.fa-check
