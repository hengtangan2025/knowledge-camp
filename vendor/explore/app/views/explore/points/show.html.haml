- @net = @point.net

.point
  :ruby
    bg = string_grid_color(@point.id)
    bg1 = ApplicationHelper::ColorTransfer.darken(bg.to_s)

  .head
    .icon{:style => "border-color:#{bg}"}
    .name
      %span= @point.name
    :ruby
      desc = @point.desc.blank? ? '没有描述信息' : @point.desc
    .desc= desc

.wall 隶属于知识网络：
%a.net{:href => url_for([@net])}
  .back
    %i.fa.fa-sitemap
  .name= @net.name
  .go
    %i.fa.fa-chevron-right

.wall 相关教程：
:ruby
  tutorials_mock2()
  tutorials = @tutorials.select { |x|
    x.related.include? @point.name
  }
.col-phone-12
  .tutorials
    = render_cell 'explore/tutorial', :list_finish, tutorials, self