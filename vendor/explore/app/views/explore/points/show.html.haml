:ruby
  bg = string_grid_color(@point.id)
  bg1 = ApplicationHelper::ColorTransfer.darken(bg.to_s)

  net_url = "/explore/nets/#{@net.id}"

.point
  .head
    .hbox
      .ibox
        .icon{:style => "border-color:#{bg}"}
      .nbox
        .name
          %span= @point.name

.tutorials
  :ruby
    desc = @point.desc.blank? ? '没有描述信息' : @point.desc
  .desc= desc

  = render_cell 'explore/tutorial', :list_with_title, @tutorials, '相关教程', self