:ruby
  tutorials_mock2()
  tid = params[:id]
  @tutorial = @tutorials.select{|t| t.id == tid}[0]

.tutorial
  - if @tutorial.parents.present?
    .parents
      - @tutorial.parents.each do |pid|
        - parent = get_tutorial_by_id(pid)
        .parent
          %span.adv 建议先掌握
          %span.t
            %i.fa.fa-warning
            %span= parent.title
  .brief
    .img= fit_image @tutorial.img, :width => '100%', :height => '100%'
  .info
    .desc= @tutorial.desc
    .enter
      %a{:href => "/explore/tutorials/#{@tutorial.id}/steps/1"}
        %i.fa.fa-arrow-right
        %span 开始学习