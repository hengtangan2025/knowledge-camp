- @nets = Explore::Mock.nets

.nets
  - @nets.each do |net|
    - color = string_grid_color net.id.to_s
    - url = "/explore/nets/#{net.id}"
    .net
      %a.box{:style => "background:#{color}", :href => url}
        .name= net.name
        .points
          .count= net.points.count