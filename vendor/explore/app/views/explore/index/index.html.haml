.nets
  - KnowledgeNetStore::Net.all.each do |net|
    - color = string_grid_color net.id
    .net
      %a.box{:style => "background:#{color}", :href => url_for([net])}
        .name= net.name
        .points
          .count= net.points.count